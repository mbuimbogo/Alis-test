const client = {};

// TODO: import and instantiate clients using client stubs that are generated from proto files.
//  For example, requests can be made to the WorkshopService via the WorkshopServicePromiseClient, conatined in workshopservice_grpc_web_pb, a file that is generated from the workshop.proto file:
//      import { WorkshopServicePromiseClient } from "@visuals.exchange/protobuf/vizx/dn/services/workshop/v1/workshopservice_grpc_web_pb";

//      // /**
//      //  * The `client` connects to the backend for frontend service and allows all calls to the api.
//      //  */
//      const client = new WorkshopServicePromiseClient(import.meta.env.VITE_Base_URL, null, null);

// client connects to the backend for frontend service and allows all calls to the api.
export default client;
